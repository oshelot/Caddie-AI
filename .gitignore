# === Flutter / Dart ===
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.metadata
.pub-cache/
.pub/
build/
**/doc/api/

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# === iOS / Xcode ===
ios/build/
ios/DerivedData/
ios/Flutter/.last_build_id
ios/Pods/
ios/.symlinks/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
.swiftpm/

# === Android ===
*.iml
*.ipr
*.iws
.idea/
.gradle/
local.properties
android/.idea/
android/build/
android/app/build/
android/app/debug/
android/app/profile/
android/app/release/
android/captures/
.externalNativeBuild
.cxx

# === Secrets / credentials ===
*.pem
*.p12
*.key
*.keystore
*.jks
*.env
*.env.*
.env.local
.env.*.local
credentials.json
service-account*.json
secrets/
.secrets/

# Build/run helpers that bundle real keys (must not be committed)
tool/run-ios.sh

# === Infrastructure (split to private CaddieAI-infra repo, 2026-04-30) ===
# All proprietary backend code lives in github.com/oshelot/CaddieAI-infra.
# infrastructure/ exists on disk as a nested git repo (its own remote).
infrastructure/

# === Migration / misc ===
migrate_working_dir/
.atom/
.history
.svn/
.buildlog/

# === OS / Editor ===
.DS_Store
Thumbs.db
*.class
*.log
*.pyc
*.swp
*.swo
*~
.vscode/
