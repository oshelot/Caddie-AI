# === iOS / Xcode ===
ios/build/
ios/DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
ios/CaddieAI/Secrets.plist

# Vendored frameworks (too large for git — download manually)
ios/Frameworks/
GoogleMobileAds.xcframework/
UserMessagingPlatform.xcframework/

# CocoaPods / SPM caches
Pods/
.build/

# === Android ===
*.iml
.gradle
local.properties
android/.idea/
android/build/
android/app/build/
android/captures/
.externalNativeBuild
.cxx

# === Secrets & credentials (both platforms) ===
*.pem
*.p12
*.key
*.keystore
*.jks
*.env
*.env.*
.env.local
.env.*.local
credentials.json
service-account*.json
secrets/
.secrets/

# === Infrastructure (split to private CaddieAI-infra repo, 2026-04-30) ===
# All proprietary backend code lives in github.com/oshelot/CaddieAI-infra.
# infrastructure/ exists on disk as a nested git repo (its own remote).
infrastructure/

# === OS / Editor ===
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
